package com.epam.cdp.volha_ivanova_task1;

public class Runner {

	int [] arr1;
	//int tempMax;
	//int tempMin;
	//int minPos;
	//int maxNeg;
	
		
	public static void main(String[] args) {
    int [] arr1 = new int [20];             // declare massive and its length
    int min = -10;
    int max = 10;
    int j = min;
    
   
   
    for (int i=0; i<arr1.length; i++) {     // filling massive
    	while (j <= max) { 
    		if (j!=0) {
    	arr1[i] = j;
    	j++;
    	//System.out.println(arr1[i]);
    	}  	else {j++;}
    	}
    }
    int sum = 0;
    for (int i=1; i<arr1.length; i++) {
    	if ((i+1)%2 == 0) {
    		sum+=arr1[i+1];
    	}
    }
    	System.out.println(sum);
    
    
   /* int maxNeg = 0;
    for (int i=0; i<arr1.length; i++) {    // find maximum negative value
    	 
    	if ((arr1[i] < 0) && (arr1[i] < maxNeg)) {
    		maxNeg = arr1[i];
    		
           	System.out.println(maxNeg);
    	}
    	
           
       	int minPos=10;                      // find minimum positive value
    	for (int b=1; b<arr1.length; b++) {
    		if (arr1[b] > 0) {
    	if (arr1[b] < minPos) {
    		minPos = arr1[b];
    	}
    	} else {b++;}
    	//int tempMin = arr1[minPos];
    	System.out.println(minPos);
    	//arr1[maxNeg] = tempMin;
    	//arr1[minPos] = tempMax;*/
	
	
   // for (int i = 0; i < arr1.length; i++) {
		//System.out.print(arr1[i] + ' '); 
    	}

}
	
	
